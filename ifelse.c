#include <stdio.h>

int main() {
    int nilai = 91; // Deklarasi dan inisialisasi nilai
    
    if (nilai >= 80) { // Jika nilai >= 80, cetak "Nilai A"
        printf("Nilai A\n");
    } else { // Jika tidak, cetak "Nilai bukan A"
        printf("Nilai bukan A\n");
    }
    
    return 0;
}
