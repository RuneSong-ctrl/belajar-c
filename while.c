#include <stdio.h>

int main() {
    int i = 0;  // Inisialisasi variabel i

    // Loop while i is less than 10
    while (i < 10) {
        i += 1;  // Tambahkan 1 ke i setiap iterasi
    }

    return 0;  // Program berakhir
}
